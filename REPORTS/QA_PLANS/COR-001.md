# QA Manual Test — COR-001 Implement Transaction Endpoints (BE)

Links
- Task Detail: ../../TASKS/DETAILS/COR-001.md
- BE Doc: ../../../Finomi.App.Api/docs/transactions.md

Status: not-tested
Comments:

Steps
1) POST `/api/v1/Transaction/deposit` body `{ "userId": <id>, "amount": 100, "paymentMethodId": <pmId> }` → 201, Location header points to `GET /api/v1/Transaction/{transactionId}`.
2) POST `/api/v1/Transaction/withdraw` body `{ "userId": <id>, "amount": 50, "paymentMethodId": <pmId>, "portfolioAllocationId": 0 }` → 201.
3) GET `/api/v1/Transaction/{transactionId}` → 200 DTO.
4) GET `/api/v1/Transaction/user/{userId}` with filters → 200 paged results.

Expected
- 201 on creates with valid response; GET by id returns matching transaction.

---

